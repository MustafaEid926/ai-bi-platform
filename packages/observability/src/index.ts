import {randomUUID} from 'node:crypto'; import type {Request,Response,NextFunction} from 'express'; export function requestContext(req:Request,res:Response,next:NextFunction){const id=req.header('x-request-id')??randomUUID(); res.setHeader('x-request-id',id); (req as any).requestId=id; next();} export const health=(name:string)=>(req:Request,res:Response)=>res.json({status:'ok',service:name,timestamp:new Date().toISOString()});